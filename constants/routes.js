const BASE_URL = "http://localhost:8080";

const API_ROUTE_PATHS = {
    PRODUCTS_BASE_URL: `${BASE_URL}/api/v1/products`,
    USERS_BASE_URL: `${BASE_URL}/api/v1/users`,
    ORDERS_BASE_URL: `${BASE_URL}/api/v1/orders`,
};

module.exports = {
    BASE_URL,
    API_ROUTE_PATHS,
};

